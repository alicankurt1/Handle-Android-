<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alican.graduationproject1">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GraduationProject1">
        <activity android:name=".MySettingsActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AllPostsInMapActivity"
            android:label="@string/title_activity_all_posts_in_map" />
        <activity android:name=".ProfileActivity"
            android:theme="@style/Theme.Handle.NoActionBar" />
        <activity android:name=".PostsOffersActivity" />
        <activity android:name=".MyOffersActivity" />
        <activity android:name=".MyPostsActivity" />
        <activity
            android:name=".PostLocationActivity"
            android:label="@string/title_activity_post_location" />
        <activity android:name=".PostDetayActivity" />
        <activity
            android:name=".SelectLocationActivity"
            android:label="@string/title_activity_select_location" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.Handle.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>